---
name: fuben-review
version: 3.1.0
description: "人生副本成稿三方审核：事实锁/因果台账 + 设定卡散文层年份对账 + 爽点密度与节奏 + 拆书机制 + 联网事实复核 + 普通观众可读性。VERDICT: PASS 前不交付。v3.1：新增「语义守恒」与散文层交叉对账（73 号聚会照/单反双出身教训固化）。"
---
# fuben-review：人生副本三方审核 v3

你是审核员，不是作者。审核的第一目标是找出「前后事实不一致」和「观众看不下去（没有爽点、节奏太慢）」，不是把所有稿子压成同一种八拍。

**v3 新增**：事实全绿但零爽点的稿子＝失败稿。没有 `## 爽点表`、或爽点表锚点无法在正文定位、或密度/峰值位置不达标的，直接 `VERDICT: FIX`，不得因为「事实严谨」放行。

## 输入与硬门

输入是 `作品/NN_xxx/设定.md` + `正文.md`。设定缺 `## 事实锁`、`## 主线`、`## 因果/状态台账`、`## 爽点表`、`## 结构验收` 任一节，直接 `VERDICT: BLOCK`。**R9 起新稿加严**：缺 `## 场面表` 或无「精读：拆文库/NN…场面写法复述」行 = BLOCK（09-18 前的旧稿豁免，只补账不返工）。规则/习惯题可以不写报应表，但必须有见证人/对照者，不能因此跳过事实、状态和爽点检查。

## Phase 0：机械预检

```bash
python3 scripts/fuben_run.py 作品/NN_xxx/
# v3.1 起 fuben_run 已内置 L1.6；单独复跑：
python3 scripts/fuben_setting_years.py 作品/NN_xxx/  # 设定散文层年份 vs 事实锁/正文
# 或逐项：
python3 scripts/fuben_loop.py design 作品/NN_xxx/设定.md
python3 scripts/fuben_loop.py facts 作品/NN_xxx/
python3 scripts/fuben_loop.py draft 作品/NN_xxx/
python3 scripts/fuben_loop.py review 作品/NN_xxx/
python3 scripts/fuben_hype.py 作品/NN_xxx/
python3 scripts/fuben_lint.py 作品/NN_xxx/ --fix-list
node skills/story-review/scripts/check-ai-patterns.js --check 作品/NN_xxx/正文.md
```

任一 `BAD` 或 blocking，报告至少 S2；事实、玩法、日期、票数、金额、人物知情面矛盾为 S1。

## Phase 1：事实账与前后逻辑

逐项从正文标行号，不凭印象：

1. **玩法唯一**：彩票题只能有一个玩法。福彩3D不得出现双色球红球/蓝球；「每天一张」必须是一张新票，不得突然每天10元、五注或倍投。
2. **数字守恒**：单位、金额、票数、次数、余额前后一致。把「每天2元」与「七年总额」写算式；精确日期遇闰日不能用约数冒充精确数。
3. **随机/结果边界**：中奖或结果必须在公布/文件/客观动作后确认。禁止把「有感觉、预感、天选」当因果。
4. **时间线闭环**：抄出所有事件年份、日期、阶段词，按出现顺序核对；倒退或无触发的跳年为 S1/S2。
5. **状态台账**：每拍必须存在触发、动作、物件/数字和状态前后；只有年月标题没有状态改变为 S2。
6. **知情面**：角色在第一次看见票、账本、消息、结果之前，不得依赖该信息行动。
7. **物件链**：关键物件必须有首次出现、旁人再次触碰、结尾回收；缺一段标 S2。
8. **人物指代**：每个「他/她/这个」在两行内能唯一指回身份；多名同性别人物同时出现就必须重报名字/身份。
9. **语义守恒（v3.1，算术过了不算过）**：钩子里的每一个计数（只有三张/没中过/一次没还），必须和正文**全部**「留/存/删/收/放回/带走」类动词逐一核对——物件被「留了」在哪（群相册≠手机相册）、被谁看见、结尾计数是否把它算进去。73 号「聚会那张你留了」被 12437−3 的算术对账放过，观众却一定在评论区抓到。凡正文出现与钩子计数**可能共域**的物件动作，必须写明容器（群相册/收藏夹/打印机），否则标 S2。
10. **设定卡散文层对账（v3.1）**：主线、节拍、台账、验收等散文句里的「物件+年份/金额」与事实锁逐项一致——58b 的 217/218、73 的「单反从 2015」都是散文层第二个出身。已脚本化：`scripts/fuben_setting_years.py`（进 fuben_run L1.6），但该工具只认「物件词邻域年份」，**人审仍需通读散文节一遍**，工具绿不代表语义绿（见第 9 条）。

## Phase 2：拆书库机制对照

### 2.1 人生副本骨架

读取 `skills/story-short-write/references/genre-styles/人生副本_通用骨架.md` v3 与 `人生副本实录.md` v4，逐节列：位置%、情绪值、正文行号、触发、状态变化、拍尾动作。**逐条核对爽点表**：锚点是否真的落在那句话上、声明位置与实际位置是否相差 ≤8%、第一个爽点是否 ≤22%、峰值（≥+5）是否落在 25%–48%、相邻爽点间距是否 ≤22%、结尾是否落在数字或器物判词上。不得只写「都是八拍所以像」，也不得只写「参考了 04」而没有机制落地行号。

**爽点三腿一票否决（v10.8，实录包 2.2.1）**：对爽点表每一行 ≥+5（钩子除外）连问三句——①这一下**谁在场**？场面 ±10 行里有没有一个具名活人（不许「你/它」互称）？②得到的**增量可数吗、场内人看得见吗**（钱/次数/关系/时间，读者独知不算）？③有没有**结算动作**替这一下合账（到账/当众表态/还击/判词）？**任一二三答不出，该行必须降 ≤+4 或回炉正文**——「全绿没人看」的 +8 就是 78 被判「没有爽点」的病灶。另查两句：钩子行是否高于其底层事件行（借分＝改表不赦）；沉沦/代价篇的主谷是否是「自选筹码的价」，被坑型谷是否 ≤1 且非归零主因。

### 2.2 供体手法

打开设定卡列出的 `拆文库/*/拆文报告.md`、`情节节点.md` 或 `写作手法.md`。每个供体回答：

- 借的是哪个机制，而不是哪个名词？
- 机制怎样改成当前人物、规则和数字？
- 正文哪一行落地？是否误把供体金额/反派/结局带入？

`04`/`21` 的数字坠落只能在事实锁允许成瘾升级时使用；规则题若写成倍投，标 S1。

### 2.3 物件/动作呼应

呼应表每一对都要有前置行和回收行。没有前置的「突然揭示」或没有改变状态的装饰回收，标 S3；事实矛盾仍优先于呼应数量。

## Phase 3：联网事实与同题方法

搜索 2–4 次，分别用于：

1. 规则、价格、开奖/制度动作和现实功能存在性；
2. 同题人生副本样本：只比较开头是否交代身份、物件（**时间不列入必查**；开头时戳 ≥2 处直接判 S2 流水账），和结尾是否落在可见动作；
3. 需要时核对日期、闰日、金额算式。

外部文章、GitHub skill 的流量承诺不是事实来源。搜索结果进报告，不把网址和热搜黑话塞进正文。

## Phase 4：普通观众 rubric

| 项目 | PASS 标准 | 失败级别 |
|---|---|---|
| 首屏 | 前5行知道谁、做什么、关键物件/规则、要等什么 | S1 |
| 流水账扫描 | 钩子豁免版（v10.1）＋R20 修订：前5行允许且仅允许 1 处独立钟面行（冷开场合法）；第6–16行禁整行时戳；时戳开头行≤3.5% 为机械闸。**全篇整行时戳计数改报告制**——一日编年/病程题钟点即题材（11 原文 4 处先例），闸报数、人审判「时间是不是信息」 | S2 |
| 围观/品牌裁决（R20 人审两项） | 围观对位与行为事实（「所有人都看着你」「再也没有人回应你」）不算宣判，闸已让行——人审只拦叙述者宣判词；品牌出现（闸报告制）按「题材主体合法、装饰删」裁决（15 iPhone／23 Mac 合法；64 豆包该删） | 人审 |
| 数字预算（advisory） | 非钱题密度 ≤25/千字且每个数字答得出「在指控谁」；钱题 ≤30。超了逐个答辩，答不出的列进 FIX 清单（实录包 3.6；不设硬闸，勿为下限注数） | S2 |
| 放大事件（advisory） | 存在一件连续主线场景 ≥10% 篇幅按拍逐写；只有压缩蒙太奇、最狠冲突一掠而过 = FIX（实录包 3.7） | S2 |
| 线性与倒叙 | 默认第一天推进；倒叙有明确路标且不让人猜 | S1 |
| 主线 | 能一句话复述「你在做什么、等什么」 | S1 |
| 因果 | 每次换时间/地点/金额都有触发和状态变化 | S1 |
| 事实 | 玩法、频率、单位、号码、日期、数量可对账 | S1 |
| 对白 | 只保留改变动作的句子；旁白稿对白稀缺 | S2 |
| 称呼 | 人物、物件、事件首次有明确称呼 | S2 |
| 爽点 | 情绪节点 ≥4.0/千字、正向爽点绝对数 ≥2（与 fuben_hype 同口径，v10.8 并线）、峰值 25%–48%、无 >22% 死区 | S1 |
| 见证结算率（v10.8） | 三腿齐全的 ≥+5 行数 ÷ ≥+5 总数；<2/3 ＝ FIX（78 实算：非钩子高爽 7 行，仅 1 行有在场者（随即反噬），三腿齐全 0 行 → 0/7——这就是「没有爽点」的数） | S1 |
| 节奏 | 前 22% 内出现第一个爽点；没有连续两节只换年份不换状态 | S1 |
| 高潮 | 按题材选择客观核对、账本对账、兑现围观、停止或人物对质；不许以「这题不该有高潮」为由留白 | S1 |
| 结尾 | 落在新状态和一个动作/物件，不是作者宣判 | S2 |
| 皮肤密度（advisory·R13b 降级） | 原文全库带 0.0–15.0（中位 3.8），「≥6」旧线系 06 单篇体质、已废。评审只看具体问：**这篇有没有一个写得比别处大的瞬间**（人审一问，计数不进结论、不作退回理由）；计数仅 `check_fuben_texture.py` 带位参照 | 人审一问 |
| 听感 QA（A3 一票制） | 发布稿必须附 TTS 通读结论；数字硌耳朵未处理 = BLOCK 发布（审稿本身不拦）。**v10.3·R11 起按「一遍听懂三问」执行**：①哪里要回听 ②哪里要脑补因果 ③哪里念出来不像人话——逐处标记，一稿 ≤3 处（阈值待 D 批校准），超出回写正文；标记数与改法写进审核报告，未写 = 视为没做听感 QA | 发布门 |
| 首屏显微镜 + 钩子评分（v10.5） | 定稿后单独读前 10 秒（≤64 字）逐字过：首句是否具体的人做具体的事、停顿点落在哪；给钩子强度 1–10 分并写理由，≤6 分首句回炉（打的是首句，不动爽点位置表）。源：huashu-douyin-script/claude-watch | 人审 |
| 数据判读三规则（v10.5，D0 截图问题制度化） | 复盘/回填后台数据时：①快照必标 T+ 时长，<T+24h 只记账不判定；②两端值（CTR 0%/100%、完播>100%）先按口径异常复核（小样本/主页口径），拿到流量来源页前不得当证据；③完播 proxy 一律=平均播放时长÷成片实长（分母用实测秒数，不用字数推算） | 复盘门 |

## Phase 5：并排读自批判（R9，学 OnlyShot 阶段 5）

与同型拆文库原文（`拆文库/NN_*/原文/原文.txt`）各取 300 字并排贴进审核报告，列「原文有而本稿没有」清单——限五类：温度/气味皮肤、动作链（动词接动词）、在场者反应、时间膨胀点（一个瞬间写 ≥12 行）、说书人钩子（「看起来一切正常/直到」式）。每条 finding 必须给「改法或删此要求的理由」。密度/口径类数字一律现场跑 `python3 scripts/fuben_density.py`，禁止引用稿件自报数。

## 输出格式

写入 `作品/NN_xxx/审核报告.md`：

```text
=== 人生副本三方审核 v2 ===
Target: 作品/NN_xxx/
Search: N 次

## Phase 0 机械预检
(design/facts/draft/review/lint/AI-patterns)

## Phase 1 事实与逻辑
| severity | 行号 | 证据 | 问题 | 修复 |

## Phase 2 拆书机制对照
| 供体/样本 | 借用机制 | 正文行号 | 是否越权 |

## Phase 3 方法与搜索复核
(规则来源、同题钩子、结尾动作)

## Phase 4 Findings
- S1/S2/S3：category / location(文件+行) / evidence(短引文) / impact(观众侧影响) / fix(必须达到的修订结果) / owner

## VERDICT: PASS | NOTES(不阻断建议) | FIX(拍号列表) | BLOCK | PROVISIONAL(关键输入不足，写明缺什么)
```

任一 S1 → BLOCK；S2 两项以上 → FIX；S3/建议 → NOTES；修完必须重新跑机械门禁。规律性问题连续出现在两篇以上，回写 `人生副本实录.md` 变更记录和脚本，不只改某一篇。

### R17 偷师三条（zenstory drama-skills 2059★ 审查纪律 + shuohao 分级语义，均 advisory）

- **审查与修改分离**：审核报告只定位问题、影响与必须达到的修订结果，不在同一轮替 owner 改来源文件。
- **独立 reviewer 优先**：条件不允许时自检，但报告首行如实标注「自检」。
- **无证据不打分**：不许只写「AI 味」「不够电影感」或给无证据分数；每条 finding 必须带位置与短引文，给不出的删。
- **分级语义映射**：BLOCK=FATAL（结构/设定层，打回重立设定）→ FIX=SEVERE（正文层，改后复跑机械闸）→ NOTES=MINOR（不阻断，下篇吸收）→ PROVISIONAL=关键输入不足（缺信源/缺设定/缺数据），写明缺什么、何时补，不得硬判。
- **成片阶段（TTS/剪辑后、发布前）**：过 `references/成片逐镜清单.md`——chaosinu7 铁律「渲染成功、脚本零报错、自动质检全绿，但成片是坏的」，逐镜看是唯一兜底，清单是人工项无闸。


## 返修轮回填诚实条款（Round 6 血泪，2026-09-19）

返修改正文后重算爽点/位置/字数时：**整表重新生成、单次写盘、写后立即 grep 逐项抽验新值**；禁止「逐行替换+整表重编号」两遍扫描（互相污染且断言死在写盘前=全部编辑静默丢失）。±8% 容差会让失真声明照样过闸——**闸绿不等于回填真实**，审核报告必须核对「密度核算行声明的总字数 == 正文实测字数」。