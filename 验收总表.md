# 验收总表 —— 面试任务书 PR A/B/C（2026-09-19）

六条均可打勾复核；每条附复跑命令。北极星提醒：门禁全绿不是终点，终点是 71 v2 发布后逃离 10–20 赞档（可证伪预测见 `作品/71_只会按字面意思办事的新人/v1_vs_v2_对比.md` 第四节）。

## PR A · 机械层

- [ ] **A1 拆文库审计范围感知 + CI**：`python3 scripts/audit_analyze_lib.py` → 「短篇契约 OK: 50 ｜ 长篇 OK: 2 ｜ PENDING: 2，BAD count: 0 / 54」，退出码 0；`.github/workflows/audit.yml` 存在且含本命令；`拆文库/_audit_scope.json` 登记 4 个长篇条目且每条有 reason。
- [ ] **A2 实测↔文档一致**：README 合规段已无手工「45/45」数字，改为 CI 机制声明；`拆文库/乱葬岗里的姑娘/_meta.json` 的 `word_count=17312` 且带 `word_count_note` 样本来源。

## PR B · 结构层

- [ ] **B1 质地闸校准双标准**：`python3 scripts/fuben_calibrate_texture.py` → 末行「验收双标准: PASS」（原稿复合通过率 ≥80%，实测 84.6%；66–72 已知失败批次拦截 8/8，71 号重写件单列「重做合规件 PASS」——重写稿失败不误拦、合规稿不被误杀）；阈值落盘 `scripts/fuben_texture_thresholds.json` 且每项带样本来源。
- [ ] **B2 颗粒度传感器接入流水线**：`python3 scripts/fuben_run.py 作品/71_只会按字面意思办事的新人/` → 十道全 PASS（含 L2.8 质地 / L2.9 颗粒度）；对 66 号实跑 `python3 scripts/fuben_granularity.py 作品/66_寝室里什么都不争的那个女生/正文.md` → FAIL（数字 0/化率 0.0/对话 7.6%）。
- [ ] **B3 反 Goodhart 波形闸**：`python3 scripts/fuben_waveform.py --scan "作品/6[4-9]_*/" "作品/7[0-2]_*/"` → 66/67/70/71v6/72 显示 5/5 连环签名；`作品/_波形.jsonl` ≥7 条且末条为 71 v7（差异 ≥2 维「打断链」）。
- [ ] **B4 版本链与依赖锁**：`人生副本实录.md` 头标 v11 且 §4.3 存在；`fuben-review/SKILL.md` version 3.1.0 且 Phase 0 含 texture/granularity/waveform 三条命令；`python3 -c "import hashlib;print(hashlib.sha256(open('skills/story-review/scripts/check-ai-patterns.js','rb').read()).hexdigest())"` 与 `skills/_manifest.json` 一致。

## PR C · 实验 + P2

- [ ] **C1 71 v2 全闸 PASS + VERDICT PASS**：上述 B2 命令十道 PASS；`作品/71_只会按字面意思办事的新人/审核报告_v2.md` 末行 `## VERDICT: PASS`；`旧版/` 内存 v6 设定与正文；`diff <(sed -n '/## 事实锁/,/## L0/p' 设定.md) <(sed -n '/## 事实锁/,/## L0/p' 旧版/设定_v6爽点紧凑.md)` 为空（事实锁红线未动）。
- [ ] **C2 可证伪预测落档 + P2**：`v1_vs_v2_对比.md` 含「<2 倍且 <100 赞则瓶颈不在脚本」的证伪条件；`python3 tests/test_gates.py` → 6/6 passed；`docs/仿写合规边界.md` 存在；根目录无 `test.txt`。

## 一键复跑（全量）

```bash
python3 scripts/audit_analyze_lib.py && \
python3 scripts/fuben_calibrate_texture.py | tail -3 && \
python3 scripts/fuben_run.py 作品/71_只会按字面意思办事的新人/ && \
python3 scripts/fuben_waveform.py --check 作品/71_只会按字面意思办事的新人/ && \
python3 -m pytest tests/ -q
```

预期：audit BAD 0；校准双标准 PASS；fuben_run 十道 PASS；波形「打断链」；pytest 6 绿。
