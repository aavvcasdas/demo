# 面试实操环节 · 候选题面

**岗位**：抖音运营 Agent（剧本生成流水线方向）
**形式**：机试 3.5 小时 + 口头追问 15 分钟
**环境**：本仓库（`aavvcasdas/demo`，人生副本流水线）+ 你惯用的 AI 工具。允许使用任何 AI 辅助，但每一处 AI 产出必须在交付说明里声明"哪段是 AI 写的、你核验了什么"——这不扣分，不声明按作弊处理。

---

## 背景设定（唯一新增事实，其余自查仓库）

你是本项目的接手人。账号主今天早上告诉你一句话：

> 「所有视频现在只有 10–20 赞。生成的剧本挺烂的，目前的技术找不到原因。」

而仓库里 `作品/_数据.csv` 写的是 57=60+（疑似推流）、62=90，`作品/_复盘_57-65_数据归因.md` 甚至写着 57/62 "过百"。
**三个数字来源互相矛盾。你的第一件事是搞清楚该信谁，最后一件事是回答"烂在剧本还是烂在账号"。**

你上一轮的书面分析我们认可，本轮考察的是：**你写的那套诊断，你自己能不能落地。**

---

## 任务一（45 min）：数据与口径 —— 先让"找不到原因"变成"能找到原因"

**交付物**（写进仓库，路径自定但需在答案里列出）：

1. **口径核验清单**：三个数据来源（账号主口述 / `_数据.csv` / 复盘 md）的矛盾点逐条列出；你下一步会去取哪些后台证据来仲裁（具体到抖音后台的哪个页面/哪个导出项）；在你拿到证据之前，哪份数据按什么折扣使用。
2. **`_数据.csv` v2 schema**：列名 + 每列的采集来源 + 它能回答哪个问题。设计标准只有一条：**新表必须能区分"死在冷启动池（没给量）"、"死在首帧/3秒（给了量没人留）"、"死在中段（留了但没看完/没互动）"这三种死法。** 现表只有点赞，请说明点赞列还能不能救、怎么救。
3. **7 天最小采集计划**：发什么（存量未发的 64/65？新稿？）、怎么发（发布参数哪些必须固定）、每天记录什么、第 7 天能得出什么结论、得不出什么结论。

**加分**：指出仓库现有数据里除了"账号主 vs CSV"之外，还隐藏着第 4 处自相矛盾的数据（提示：仔细对 `_数据.csv` 的备注列和复盘 md）。

---

## 任务二（90 min）：修一篇稿 —— 把你诊断出的病因亲手治好

对象：`作品/58_反骨嘉豪`（30 赞，`_复盘_57-65` 定性："没有代价，收束给了糖，视角是'嘉豪传'套了'你'"）。这是全库证据最完整的失败样本。

**Step 1（20 min）丢失规则清单**
本仓库 09-13 的复盘曾把 5 条"剧本层病灶"（撞正主 / 无代价 / 无单一放大事件 / 视角错位 / 抒情收束）宣称"已全部写入 skill"。请对照 `skills/story-short-write/references/genre-styles/人生副本实录.md`（现行版，§0–§5）与 `人生副本实录_v1_归档.md`，逐条列出：这 5 条规则在现行包里**还在不在**？给出两边的小节号或"缺失"结论。写 `作品/_丢失规则清单.md`。

**Step 2（45 min）重写 58b**
新建 `作品/58b_反骨嘉豪_代价版/`（`设定.md` + `正文.md`，可含其他你需要的文件）：

- 必须过 `python3 scripts/fuben_run.py 作品/58b_反骨嘉豪_代价版/` 七道闸全 PASS；
- 同时必须逐条规避 Step 1 清单里的 5 条病灶——**闸是底线，不是目标**。我们的门禁查不出剧本层问题，查得出的部分过了不代表能打；
- 主题材不变（反骨的人），单篇 ≤2500 字。

**Step 3（15 min）自检表**
五病灶逐条：这版怎么解决的，引用你正文里的行号/句子作证。

**Step 4（10 min）可证伪的假设卡**
一段话：你预测这版相对 58 在哪个指标上有几量级的变化（用任务一定义的 schema 字段说）；**如果发出去仍然 ≤20 赞，你的哪个假设被证伪，下一步改什么。** 写"如果失败说明什么"比写"我会成功"重要。

**红线**：不许改动 58 原稿目录任何文件；不许为过闸机械注水（我们会比对 58b 与 58 的信息增量）。

---

## 任务三（45 min）：工程题（三选一，现场写代码，可查资料）

选一个做完、做实，不要三个都摸：

- **A. 供体污染隔离**：给 `scripts/audit_analyze_lib.py` 加 `--scope=fuben|long|all`（默认 fuben），把 `拆文库/` 里 9 个长篇网文条目隔离出 fuben 校验口径；同步把 README 里已过期的"45/45 通过"改成与新行为一致的真话。验收：`--scope=all` 输出的 BAD 数与你实际看到的一致。
- **B. 门禁回归测试**：为 `fuben_run.py` 建最小 pytest 回归集：旧版 72（`作品/72_每天买一张彩票的人_重生成基线/`）必须 FAIL 且**断言具体失败层级/错误**（不是只断言退出码非零）；`72_爽点紧凑版` 必须 PASS。测试要能在 30 秒内跑完。
- **C. 阈值单一事实源**：从 `scripts/fuben_hype.py` 抽出至少 3 个硬阈值（如首个大事件 ≤32%、峰值区间、节点密度）做成配置文件（json/yaml 任选），脚本读配置跑通原校验，配置与代码不再双写。

**评分重点**：真测试 vs 摆设；提交粒度（我们看你的 git commit 怎么切）；不顺手重构全库。

---

## 任务四（30 min）：给账号主的一页纸

写 `_一页纸_10到20赞归因.md`，直接回答账号主那句话："剧本烂还是账号烂？" 要求：

1. 列三个候选原因（账号/流量权重、视频产品层——首帧/配音/画面/时长、剧本层），每个附上仓库内已有的证据（引文件与行号）或"暂无证据"；
2. 每个候选原因配一个**本周内可执行、成本可忽略**的判别实验；
3. 本周"停做什么 / 开始做什么"各不超过 3 条；
4. 如果你认为现有数据不足以归因，**直说**，并把任务一的采集计划作为"先造观测、再谈归因"的第一步。

---

## 口头追问（15 min）

围绕你的书面答卷与本轮实操产出提问，方向包括但不限于：数据矛盾如何仲裁、单稿过拟合在小样本下的替代验收标准、你答卷里数字的精确性、供体污染的存量处理。**答错不扣大分，编造扣双倍。**

---

## 评分（100 分，权重公开）

| 任务 | 分值 | 一票红线 |
|---|---|---|
| 一 数据与口径 | 25 | 编造后台数据；把点赞样本当完播结论用 |
| 二 修 58b | 35 | 改了 58 原稿；七闸有 FAIL 谎称 PASS；注水凑闸 |
| 三 工程题 | 20 | 测试/配置是摆设（断言不出真实行为） |
| 四 一页纸 | 20 | 给出"就是剧本烂/就是账号烂"的二选一断言而无判别设计 |

**录用线**：总分 ≥70 且任务二 ≥24 且无红线。任务二 Step 1 若独立发现我们埋的那个关键问题，该题直接满分。
