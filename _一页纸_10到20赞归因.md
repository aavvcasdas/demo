# 一页纸：「剧本烂还是账号烂？」

> 给账号主。2026-09-18。数据全部可在仓库内复核（文件+行号）；没证据的地方我直接写"暂无"，不装懂。

## 先回答能不能判：**今天判不了，但差得不远**

「所有视频只有 10–20 赞」这句话本身要先拆：仓库里点赞**只进不退**，而 `作品/_数据.csv` 09-13 记的 57=60、62=90——所以 10–20 只能是**最近这批（66–72 时代，CSV 没记）**，不是"所有"。这个时间轴错位是"找不到原因"的第一半原因：我们一直在拿不同批次的数互相比，还都没有播放量做分母（CSV 播放列六行全 0）。第二半原因是仓库里三个候选病因的证据厚度完全不对称——见下表，**每个病配一个本周零成本判别实验，做完（主要是把后台数补进来）就能判**。

## 三个候选原因与证据

### 1. 账号/流量权重层 —— 仓库内：**暂无直接证据**（无后台导出）
- 间接迹象：`作品/_数据.csv` 57 行备注「疑似推流」——当时就有人察觉流量分配不自然；09-13 快照 30–90 → 09-18 口述 10–20，整批塌到同一低带，更像"每条都没拿到量"而不是"每条都难看"（难看的内容点赞率会分化，不会一起趴在 10–20）。
- **实验 A（半天，0 元）**：账号主做三件事——①创作者中心「账号诊断」+ 查系统通知有无违规/限流记录；②把 66–72 **每条**的播放量/来源占比截图发来（先别发新稿！）；③查这几条是否有一条播放 >2000。判读：**全批播放 <500 → 账号侧实锤，修稿暂停两周**；有一条起量 → 账号仍在分发，主因转向 2/3。
### 2. 视频产品层（首帧/配音/画面/时长）—— 仓库内：**只有定性证据，零量化证据**
- `人生副本爆款分析_深度解析报告.md` §四.4：文案再好，"固定开场+齿轮音效+画面+BGM+字幕节奏跟不上同样完播崩"；且仓库从未记录过 duration_sec/配音参数（v2 表新建这两列就是为此）。
- 一个内部矛盾暴露规格漂移：v1 归档 §十一.6（L241）说硬指标 2,800–3,500 字，现行 draft 闸却是 2,200–3,600、深度报告建议 ≤2,000/3 分钟——**字数目标三代不一，成片时长就没人对齐过**。
- **实验 B（0 元）**：拿 58b 成片与任一 10w+ 对标视频各看 3 遍：0–3 秒的信息密度/音量/首帧差异由人打分记录进 `_数据_v2.csv` 的 `成片版本` 行；更硬的做法已并入任务一采集计划——`retention_t5s_pct <45%` 即产品层首帧实锤。
### 3. 剧本层 —— 仓库内：**证据最密，但有反例**
- 实锤一：09-13 复盘声称"5 条病灶规则已全部写入 skill"，实测**现行 v10 包里 4 条半已丢失**（`作品/_丢失规则清单.md`：撞正主/视角位整体丢、代价账本半丢、放大事件被改成位置闸；`_复盘_57-65` L37–40 引用的 §15.3/15.4/15.5 小节号只存在于 v1 归档）——**58 那类病因的规则修复在现行流程里等于没修**。
- 实锤二：45–54 同质克隆、糖水选题（深度报告 §四.1，仓库自查承认）。
- 反例（也是我最担心的）：v7–v10 五连改全部由 72 一篇稿+单人反馈驱动；改完后 66–72 机检全绿，**口述数据反而更差**。两种读法：修的方向过了（不是主因）或修错了（单稿过拟合）。**没有后台数据，这两读法在仓库内不可区分——这正是不能拍板"就是剧本烂"的原因。**
- **实验 C（并入 7 天计划，0 元）**：D1–D5 五条稿发布后，用预注册三分判定（`作品/_口径核验与采集计划_v2.md` §三末）：play 正常+t5s 低 → 制作层；play 低 → 账号层；play/t5s 正常但 cliff 落在无爽点区 → 剧本层实锤，回改 fuben_hype。

## 本周停 / 开始（各 3 条）

**停**
1. 停"机检全绿=可发"——发布前必须过 fuben-review 人审并落 `审核报告.md`（64 的 S1 品牌就是机检全绿人审才抓出的）。
2. 停单条反馈升级规则：v11 起，任何观众/单人反馈先进"假设池"，**两篇复现或后台数据方向一致才进包+闸**（防 72 式过拟合）。
3. 停引用"过百/最差"这类数字下任何结论——包括复盘里的旧措辞（已证与 CSV 备注矛盾，见 `_口径核验与采集计划_v2.md` §1.5 #4/#5）。

**开始**
1. 开始 D0：把 66–72 后台数补进 `作品/_数据_v2.csv`（实验 A 的前半，今晚就能做完——**这一步可能直接出答案，不用等 7 天**）。
2. 开始 D1–D5 五天发布计划（同参数控制，见采集计划 §三），每条 T+2h/24h/7d 快照。
3. 开始把 `_丢失规则清单.md` 的 4 条半丢失规则回写现行包（下周一提交包 v11 草案，先回写"同题差异/视角位/代价账本"三条最硬）——**这件事无论归因结果如何都是对的**，因为丢规则是仓库内已证事实，不依赖任何后台数据。

## 一句话给账号主

现有数据不足以在"剧本烂/账号烂"里选一个——**谁现在给你拍板选哪个，谁就是在编**。但不用悲观：病因没藏在"技术找不到"里，藏在**没人记录播放量和快照时间**里；把 66–72 的后台数补进表（一个晚上）+ 五天控制发布（一周），7 天后我们讨论的就不再是"为什么扑"，而是"下一步改哪层"。
